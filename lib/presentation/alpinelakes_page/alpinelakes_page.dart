import 'package:waran/screens.dart';

class AlpineLakesPage extends StatelessWidget {
  const AlpineLakesPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text(
          "AlpineLakesPage",
        ),
      ),
    );
  }
}
